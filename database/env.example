# Database configuration for the standalone Postgres compose in this directory.
POSTGRES_DB=anchordesk              # Database name
POSTGRES_USER=mtapp                     # Username
POSTGRES_PASSWORD=changeme              # User password
